interface:
  display_name: "混音处方决策引擎"
  short_description: "把人声或完整混音诊断转成可复测的插件级操作方案"
  default_prompt: "使用 $prescribe-mix-actions 根据分析报告生成分优先级的插件、路由、参数、停止条件与复测方案。"
policy:
  allow_implicit_invocation: true

